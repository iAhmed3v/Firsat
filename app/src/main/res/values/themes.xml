<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Firsat" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorPrimary</item>
        <item name="colorSecondaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- No Action Bar Theme.-->
    <style name="Theme.Firsat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Toolbar Theme -->
    <style name="ToolbarTheme">
        <item name="android:fontFamily">@font/tajawal_extrabold</item>
        <item name="android:textSize">20sp</item>
    </style>

    <!-- Navigation View Theme -->
    <style name="NavigationViewTheme">
        <item name="android:fontFamily">@font/tajawal_medium</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>